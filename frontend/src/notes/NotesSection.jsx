/**
 * NotesSection
 *
 * Entry point for the Notes tab in TrackerPage.
 * All notes logic and UI lives in MultiNotesSection.
 */
import MultiNotesSection from './MultiNotesSection';

export default function NotesSection() {
  return <MultiNotesSection />;
}
